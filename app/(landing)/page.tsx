import HeroSection from '@/components/landing-page/hero-section';
const Home = () => {
  return (
    <main className="flex h-screen min-h-screen w-full flex-col items-start justify-center bg-color-primary overflow-hidden">
      <HeroSection />
    </main>
  );
};

export default Home;
